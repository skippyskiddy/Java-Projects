package hw2;

/**
 * An enum that lists several room types for the hotel rooms. Constructs its own values to be used
 * later in the class. Returns a hw2.Room type as an INT value to represent room capacity for each
 * room type.
 */
public enum RoomType {
  SINGLE,
  DOUBLE,
  FAMILY;
}
